import React from 'react'

import Card from '../UI/Card'

import './style.css'

const SideBar = (props) => {
  return (
    <Card style={{ width: '27%' }}>
      SideBar
    </Card>
  )
}

export default SideBar